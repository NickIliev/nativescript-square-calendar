
import { View, Property, booleanConverter, CSSType } from "tns-core-modules/ui/core/view";

@CSSType("SquareCalendar")
export class Common extends View {
    // public static checkedChangeEvent = "checkedChange";

    // public checked: boolean;

    // _onCheckedPropertyChanged(newValue: boolean) {
    //     //
    // }
}

// function onCheckedPropertyChanged(switchBase: Common, oldValue: boolean, newValue: boolean) {
//     switchBase._onCheckedPropertyChanged(newValue);
// }

// export const checkedProperty = new Property<Common, boolean>({ name: "checked", defaultValue: false, valueConverter: booleanConverter, valueChanged: onCheckedPropertyChanged });
// checkedProperty.register(Common);
